spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: dracolich-mtg-deck-builder-api
  webflux:
    base-path: dracolich/mtg-deck-builder/api/v0/
  mongodb:
    database: ${MONGODB_DATABASE}
    uri: ${MONGODB_URI}
    representation:
      uuid: standard

server:
  port: ${PORT:8080}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:}

dracolich:
  cookie:
    secret: ${ANON_COOKIE_SECRET}
    lifetime: PT24H
    secure: false
  jwt:
    public-key: ${DRACOLICH_JWT_PUBLIC_KEY:classpath:keys/ec-public.pem}

management:
  server:
    port: 7980
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true